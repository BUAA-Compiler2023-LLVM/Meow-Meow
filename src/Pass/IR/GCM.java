package Pass.IR;

import IR.IRModule;
import Pass.Pass;

public class GCM implements Pass.IRPass {

    @Override
    public void run(IRModule module) {

    }

    @Override
    public String getName() {
        return "GCM";
    }
}
